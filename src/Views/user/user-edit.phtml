<?php if (\App\Core\Access::hasRole('super_admin')) { ?>

<section class="page user-edit">
    <h1 class="page__title">Modification de l'utilisateur</h1>

    <div class="page__wrapper">
        <div class="user-edit__form">
            <form method="POST" action="index.php" class="form">
                <?= $this->getCsrfField(); ?>
                <input type="hidden" name="route" class="form__input" value="user-update-submit">
                <input type="hidden" name="id" value="<?= htmlspecialchars($formData['id'] ?? '', ENT_QUOTES) ?>">
                <input type="hidden" name="active" value="<?= htmlspecialchars($formData['active'], ENT_QUOTES) ?>">

                <h2 class="sr-only">Informations utilisateur</h2>

                <div class="form__group">
                    <label for="first_name" class="form__label">Prénom <span class="form__hint">(obligatoire)</span></label>
                    <input type="text" id="first_name" name="first_name" class="form__input" required value="<?= htmlspecialchars($formData['first_name'] ?? '', ENT_QUOTES) ?>">
                </div>

                <div class="form__group">
                    <label for="last_name" class="form__label">Nom <span class="form__hint">(obligatoire)</span></label>
                    <input type="text" id="last_name" name="last_name" class="form__input" required value="<?= htmlspecialchars($formData['last_name'] ?? '', ENT_QUOTES) ?>">
                </div>

                <div class="form__group">
                    <label for="email" class="form__label">Adresse e-mail <span class="form__hint">(obligatoire)</span></label>
                    <input type="email" id="email" name="email" class="form__input" required value="<?= htmlspecialchars($formData['email'] ?? '', ENT_QUOTES) ?>">
                </div>

                <div class="form__group">
                    <label for="password" class="form__label">Mot de passe provisoire</label>
                    <input type="password" id="password" name="password" class="form__input" aria-describedby="password-hint">
                    <p id="password-hint"class="form__hint">Laissez vide pour conserver le mot de passe actuel</p>
                </div>

                <div class="form__group form__group--full">
                    <label for="id_role" class="form__label">Rôle <span class="form__hint">(obligatoire)</span></label>
                    <select id="id_role" name="id_role" class="form__select" required aria-describedby="role-description">
                        <option value="">-- Choisir un rôle --</option>
                        <?php foreach ($roles as $role) { ?>
                            <option value="<?= $role['id'] ?>"
                                data-description="<?= htmlspecialchars($role['description'], ENT_QUOTES) ?>"
                                <?= (isset($formData['id_role']) && $formData['id_role'] == $role['id']) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($role['role_name'], ENT_QUOTES) ?>
                            </option>
                        <?php } ?>
                    </select>
                    <p id="role-description" class="form__hint">Conditions du rôle choisi :</p>
                </div>

                <div class="user-edit__buttons">
                    <button type="submit" class="btn btn--dark">Enregistrer</button>
                    <a href="index.php?route=user-list&status=<?= htmlspecialchars($_GET['status'] ?? 'active', ENT_QUOTES) ?>" class="btn btn--dark">
                        Annuler
                    </a>
                </div>
            </form>
        </div>
    </div>
</section>

<?php } ?>