<?php if (\App\Core\Access::hasRole('super_admin')) { ?>

<section class="page user">
    <h1 class="page__title">Création d’un nouvel utilisateur</h1>

    <div class="page__wrapper">
        <div class="page__form">
            <form method="POST" action="index.php" class="form">
                <input type="hidden" name="route" class="form__input" value="user-create-submit">
                <input type="hidden" name="active" value="1">

                <h2 class="sr-only">Informations utilisateur</h2>

                <div class="form__grid">

                    <div class="form__group">
                        <label for="first_name" class="form__label">Prénom *</label>
                        <input type="text" id="first_name" name="first_name" class="form__input" required value="<?= htmlspecialchars($formData['first_name'] ?? '') ?>">
                    </div>

                    <div class="form__group">
                        <label for="last_name" class="form__label">Nom *</label>
                        <input type="text" id="last_name" name="last_name" class="form__input" required value="<?= htmlspecialchars($formData['last_name'] ?? '') ?>">
                    </div>

                    <div class="form__group">
                        <label for="email" class="form__label">Adresse e-mail *</label>
                        <input type="email" id="email" name="email" class="form__input" required value="<?= htmlspecialchars($formData['email'] ?? '') ?>">
                    </div>

                    <div class="form__group">
                        <label for="password" class="form__label">Mot de passe provisoire *</label>
                        <input type="text" id="password" name="password" class="form__input" required>
                        <!-- icone pour afficher / masquer à voir si nécessaire -->
                    </div>

                    <div class="form__group form__group--full">
                        <label for="id_role" class="form__label">Rôle *</label>
                        <select id="id_role" name="id_role" class="form__select" required>
                            <option value="">-- Choisir un rôle --</option>
                            <?php foreach ($roles as $role) { ?>
                                <option value="<?= $role['id'] ?>"
                                    data-description="<?= htmlspecialchars($role['description']) ?>"
                                    <?= (isset($formData['id_role']) && $formData['id_role'] == $role['id']) ? 'selected' : '' ?>>
                                    <?= htmlspecialchars($role['role_name']) ?>
                                </option>
                            <?php } ?>
                        </select>
                        <p id="role-description" class="form__hint">Conditions du rôle choisi :</p>
                    </div>

                    <div class="form__actions">
                        <button type="submit" class="btn btn--dark">Enregistrer</button>
                        <a href="index.php?route=user-home" class="btn btn--dark">Annuler</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</section>

<?php } ?>